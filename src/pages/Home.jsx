import React, { Fragment } from "react";
import { Header, Slider, PopularMenu } from "../componenets";

const Home = () => {
  return (
    <Fragment>
      <Header />
      <Slider />
      <PopularMenu />
    </Fragment>
  );
};

export default Home;
